---
duckdns_dir: /opt/duckdns
duckdns_domain: "kaloscasshome" # subdomain only, e.g. myhome (not myhome.duckdns.org)
duckdns_token: "{{ lookup('env', 'DUCKDNS_TOKEN') }}" # set DUCKDNS_TOKEN in .env, then: source .env
